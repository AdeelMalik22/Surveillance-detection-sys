export function renderEvents(root,events){root.innerHTML=events.length?`<div class="event-table">${events.slice(0,50).map(e=>`<div class="event-row"><b>${e.object_class}</b><span>${String(e.camera_id).slice(0,10)} · ${e.zone_id}</span><time>${new Date(e.timestamp).toLocaleString()}</time></div>`).join('')}</div>`:'<div class="empty-panel"><b>No events yet</b><small>Detection activity will appear here.</small></div>'}
