export function openZoneModal(feed){const root=document.querySelector('#modal-root');root.innerHTML=`<div class="modal-bg"><div class="modal"><header><h2>Add zone</h2><button id="close-modal">×</button></header><p>Save a polygon for ${feed.filename}.</p><form id="zone-form"><input name="id" placeholder="Zone name" required><input name="polygon" placeholder="[[100,100],[500,100],[500,400]]" required><button>Save zone</button></form></div></div>`;document.querySelector('#close-modal').onclick=()=>root.innerHTML='';document.querySelector('#zone-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);const r=await fetch('/zones',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({camera_id:feed.session_id,zone:{id:f.get('id'),polygon:JSON.parse(f.get('polygon'))}})});if(r.ok)root.innerHTML='';else alert('Unable to save zone')}}
